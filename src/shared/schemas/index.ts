/**
 * @module shared/schemas
 * @description Public entry point for cross-feature Zod schemas:
 * reusable field validators and the shared params/pagination schemas.
 */
export { FieldsSchema, PaginationSchema, ParamsSchema } from './fields.schema';
